<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abc.reimbursement">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="com.google.android.gms.vision.DEPENDENCIES"
                android:value="ocr" />
        </activity>
        <activity
            android:name=".MealActivity"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".TravelActivity"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".ImageToTextConverter" />
        <activity android:name=".MiscellaneousActivity" />
        <activity android:name=".TeamExpenseActivity" />
        <activity android:name=".ChoiceActivity"></activity>
        <activity android:name=".ExpenseReport"></activity>
    </application>

</manifest>